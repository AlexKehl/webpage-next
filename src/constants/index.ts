import { Category } from '@prisma/client'
import { Language } from 'src/types'

export const Languages: Language[] = ['en', 'de', 'ru']

export const Categories: Category[] = ['acryl', 'oil', 'graphics', 'other']
